---
import type { AstroInstance } from "astro";
import Layout from "../layouts/Layout.astro";

// await glob()

interface PostInstance extends AstroInstance {
  title: string;
}

const posts = import.meta.glob<PostInstance>("./posts/*.astro", { eager: true });
for(const [path, post] of Object.entries(posts)) {
	console.log(path, post.title, post.url);
}
---

<Layout>
</Layout>

<style>
	time {
		color: gray;
	}

	h2 {
		font-size: 2rem;
		margin: 0;

		a {
			text-decoration: none;
			color: inherit;
		}
	}

	p {
		color: #2e2e2e;
		font-style: italic;
		margin: 0.5rem 1rem 3rem 0;
	}

	a {
		color: #2795b6;
	}
</style>
