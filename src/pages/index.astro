---
import { posts } from "../scripts/getContent";
import Layout from "../layouts/Layout.astro";
import PostItem from "../components/PostItem.astro";
---

<Layout>
	{
		posts.toReversed().map((post) => <PostItem post={post}/>)
	}
</Layout>